# app/domain/entities — Domain Models

Framework-free business entities and their invariants. **Empty in Phase 1** —
there is no business domain yet.

Distinct from **API Schemas** (`app/schemas`, Pydantic, transport) and
**Persistence Models** (`app/models`, SQLAlchemy, database). See TDD §8a.
